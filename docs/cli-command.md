 - Generated by [gen_docs.rs](../src/bin/gen_docs.rs), do not edit!
 - Generate date: 2026-02-27

# Zellij MCP Server CLI Commands

```text
>>> help
Usage: <COMMAND>

Commands:
  attach       Attach to an existing zellij session
  close-tab    Close current tab
  detach       Detach from the current session
  dump         Dump the current screen
  exit         Exit the interactive shell
  list         List active sessions or tabs
  new          Create a new tab
  switch       Switch to a specific tab
  rename       Rename current tab or session
  send         Send a specific key to the current tab
  set          Set some config
  show-layout  Show current layout
  status       Show MCP status
  write        Write text to the current tab
  help         Print this message or the help of the given subcommand(s)
```

## attach

```text
>>> attach --help
Attach to an existing zellij session

Usage: attach <SESSION_NAME>

Arguments:
  <SESSION_NAME>  Session name to attach to

Options:
  -h, --help  Print help
```

## close-tab

```text
>>> close-tab --help
Close current tab

Usage: close-tab

Options:
  -h, --help  Print help
```

## detach

```text
>>> detach --help
Detach from the current session

Usage: detach

Options:
  -h, --help  Print help
```

## dump

```text
>>> dump --help
Dump the current screen

Usage: dump <COMMAND>

Commands:
  screen  Dump current screen
  help    Print this message or the help of the given subcommand(s)

Options:
  -h, --help  Print help
```

### dump screen

```text
>>> dump screen --help
Dump current screen

Usage: screen [OPTIONS]

Options:
  -n, --lines <LINES>  Return the last N lines
      --begin <BEGIN>  Start line (1-indexed, must be used with --end)
      --end <END>      End line (1-indexed, must be used with --begin)
  -h, --help           Print help
```

## exit

```text
>>> exit --help
Exit the interactive shell

Usage: exit

Options:
  -h, --help  Print help
```

## list

```text
>>> list --help
List active sessions or tabs

Usage: list <FILTER>

Arguments:
  <FILTER>  Filter: "session" (or "s") | "tab" (or "t")

Options:
  -h, --help  Print help
```

## new

```text
>>> new --help
Create a new tab

Usage: new [NAME]

Arguments:
  [NAME]  Optional tab name

Options:
  -h, --help  Print help
```

## switch

```text
>>> switch --help
Switch to a specific tab

Usage: switch <TAB_NAME>

Arguments:
  <TAB_NAME>  Name of the tab to switch to

Options:
  -h, --help  Print help
```

## rename

```text
>>> rename --help
Rename current tab or session

Usage: rename <COMMAND>

Commands:
  tab      Rename current tab
  session  Rename current session
  help     Print this message or the help of the given subcommand(s)

Options:
  -h, --help  Print help
```

### rename tab

```text
>>> rename tab --help
Rename current tab

Usage: tab [OPTIONS] [NAME]

Arguments:
  [NAME]  New name for the target

Options:
  -u, --undo  Undo previous rename
  -h, --help  Print help
```

### rename session

```text
>>> rename session --help
Rename current session

Usage: session <NAME>

Arguments:
  <NAME>  New name for the session

Options:
  -h, --help  Print help
```

## send

```text
>>> send --help
Send a specific key to the current tab

Usage: send <KEY>

Arguments:
  <KEY>  Key to send (e.g., ctrl+c, enter, esc, f1)

Options:
  -h, --help  Print help
```

## set

```text
>>> set --help
Set some config

Usage: set <COMMAND>

Commands:
  log-dir  If set LogDir, all dump screen commands will log to LogDir
  help     Print this message or the help of the given subcommand(s)

Options:
  -h, --help  Print help
```

### set log-dir

```text
>>> set log-dir --help
If set LogDir, all dump screen commands will log to LogDir

Usage: log-dir <PATH>

Arguments:
  <PATH>  Path to the LogDir

Options:
  -h, --help  Print help
```

## show-layout

```text
>>> show-layout --help
Show current layout

Usage: show-layout

Options:
  -h, --help  Print help
```

## status

```text
>>> status --help
Show MCP status

Usage: status

Options:
  -h, --help  Print help
```

## write

```text
>>> write --help
Write text to the current tab

Usage: write [OPTIONS] <TEXTS>...

Arguments:
  <TEXTS>...  The text to write (multiple texts will be executed sequentially)

Options:
  -n, --no-enter  Do not add a newline at the end
  -h, --help      Print help
```

